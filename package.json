{
  "name": "transitops-root",
  "version": "1.0.0",
  "description": "Root package for TransitOps Transport Operations Platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "install:all": "npm.cmd install && npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "seed": "npm.cmd run seed --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
